{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Dev Container (local placeholder schema)",
  "type": "object",
  "description": "Minimal local schema to prevent remote schema fetch failures. Not a full spec.",
  "properties": {}
}
